#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "othello.h"


int main()
{
    //srand(time(NULL));
    //partie_2_joueurs();

    //partie_vs_computer();

    computer_vs_computer();


    /*srand( time( NULL ) );

    printf( "%d\n", rand() );
    printf( "%d\n", rand() );
    printf( "%d\n", rand() );

    
    printf( "%d\n", rand() );
    printf( "%d\n", rand() );
    printf( "%d\n", rand() );  */

    return 0;
}
